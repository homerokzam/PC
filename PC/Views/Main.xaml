<?xml version="1.0" encoding="UTF-8"?>
<t:MenuContainerPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
  xmlns:t="clr-namespace:SlideOverKit" 
  xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
  prism:ViewModelLocator.AutowireViewModel="True"
	x:Class="PC.Views.Main">
  <ContentPage.Content>
		<StackLayout>
      <Button Text="Scan" Command="{Binding OnCommandScan}" />
      <Button Text="SlideOverKit" Command="{Binding OnCommandSlideOverKit}" />
      <Button Text="Rg.Plugins.Popup" Command="{Binding OnCommandRgPluginsPopup}" />
		</StackLayout>
  </ContentPage.Content>
</t:MenuContainerPage>